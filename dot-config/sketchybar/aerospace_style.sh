#!/usr/bin/env bash

# Visual constants for the aerospace cluster (workspace icons + status
# carrier). Sourced from sketchybarrc so items/aerospace.sh sees them at
# config time, and from plugins/aerospace.sh so the plugin sees them when
# sketchybar invokes it as a subprocess.

export AEROSPACE_FONT_SIZE=22
export AEROSPACE_Y_OFFSET=1
