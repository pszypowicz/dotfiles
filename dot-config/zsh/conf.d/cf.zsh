# Cloudflare CLI completions. `cf` writes the completion script under
# ~/.config/cf/completions on first run and expects a loader like this one.
[[ -r "$HOME/.config/cf/completions/cf.zsh" ]] && source "$HOME/.config/cf/completions/cf.zsh"
